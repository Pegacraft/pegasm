# stack data types
# chars, strings, int, double
# stack write examples:c, test, 5, 1.5
# memory memr/memw
# read adds the value in the address on the stack
# write adds the value on top of stack to memory
# example: 10 0 memw/ 0 memr
# math operations
# +, -, *, /, %
# print command prints top of stack to terminal
# out

# Tests calculation
# 10 2 + out

# Tests swap
# 10 5 swap out
# swap out

# Test memory
# 10 0 memw
# 0 memr out

0 0 memw a flag 0 memr 1 + 0 memw 0 memr out 0 memr 10 < a if

